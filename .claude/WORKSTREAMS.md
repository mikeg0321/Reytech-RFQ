# Active Workstreams

One repo, one main. Track in-progress work here. Every context window reads
this before starting and updates it on branch create / PR / merge / abandon.

> **Den collapsed 2026-05-21 — Job #0.** 138 working directories → 1 canonical
> repo (`C:\Users\mikeg\rfq-spine-sequential-numbering`) + `Reytech-RFQ`.
> 1,666 branches → 58 (1,608 confirmed-merged branches pruned). The pre-Job-#0
> branch table — dozens of merged/abandoned rows back to 2026-04 — was stale
> cruft and has been removed. Audit trail of exactly what was deleted:
> `_diag/job0_branch_killlist.txt` and `_diag/job0_worktrees_removed.txt`.

## Operating model

`CLAUDE.md §0` is the law. Worktrees only — never new clones — capped at 10
live. `make worktree name=feat/topic` to create; `make worktree-list` to audit.
Only the Architect authorizes substrate / schema / migration changes (LAW 4).

## Current branches

| Branch | Status | Notes |
|--------|--------|-------|
| `main` | canonical | Never pushed to directly. |
| `fix/job1-delete-routes-cchcs-packet` | Active | Wolfpack Implementer PR-Job1-C — DELETES legacy `src/api/modules/routes_cchcs_packet.py` (329 LOC), its loader in `dashboard.py`, the "CCHCS Non-IT Packet (22pg)" button + `generateCchcsPacket()` JS in `pc_detail.html`, plus the 3 tests that pinned the deleted surfaces (`test_cc1_cc2_cc5_packet_pipeline.py`, `test_cchcs_packet_route.py`, `test_ui1_cchcs_packet_compose.py`) and the `test_cchcs_packet_route.py` line in `.github/workflows/ci.yml`. Comment-only updates to `test_utilization.py` + `src/agents/cchcs_packet_detector.py`. Spine canonical packet route survives at `src/api/modules/routes_spine.py::get_packet_pdf` (`/spine/quotes/<id>/forms/packet/pdf`); operator-facing `/rfq/<id>` surface untouched. §0 Job #1 LAW 2 "legacy CCHCS quote/package routes DELETED — commit in git log" gate advanced. Architect-authorized 2026-05-27. `dashboard.py` compiles, `pc_detail.html` Jinja parses, 10/10 `test_utilization.py` green, 8/8 `test_spine_architecture.py` green. Worktree: `rfq-job1-delete-routes-cchcs-packet`. Inspector Chrome verification on Duffey rfq_0124647e scheduled post-deploy. |
| `feat/job1-pr3-forms-render` | Job #1 PR-3 | `src/spine/forms_render.py` — the Format-B CCHCS adapter (703B/703C + 704B + Bid Package) + format-aware /forms routes. |
| `fix/flatten-regen-appearance-before-bake` | PR-10, shipping | `src/spine/flatten.py` — regenerate widget appearances (`fitz.Widget.update()`) BEFORE `bake()`. Fixes Demidenko PC comb-spacing + clipping caught 2026-05-23. Worktree: `rfq-spine-sequential-numbering`. |
| `fix/liveness-scprs-source-canonical-writer` | PR A of liveness silent-failure triage 2026-05-25 | SCPRS liveness check + `scprs_browser` writer. (1) `liveness_checks.py` — new `_multi_source_freshness` primitive; SCPRS check reads `MAX(scprs_po_master.pulled_at, scraped_at)`. (2) `scprs_browser._store_results` — writes both columns. Closes the false-alarm class: scheduled browser scrape wrote `scraped_at`, check only read `pulled_at`. Same shape as PR #1076 substrate-sourcing fix. 13/13 tests green. Worktree: `rfq-liveness-scprs-source-canonical-writer`. |
| `fix/email-poller-restartable` | PR B of liveness silent-failure triage 2026-05-25 | `routes_crm.start_polling` — register the `email-poller` thread with `register_restartable` so the job watchdog revives a dead thread. Closes the silent-death class: 4 days silent inbound with 3 unread CDCR RFQs in the sales inbox 2026-05-21→25. Mirrors the pattern used by the 4 other daemons in `dashboard.py` (follow-up-engine, award-tracker, oracle-weekly-report, cross-sell-weekly-digest). 3/3 new tests + scheduler substrate verified. Worktree: `rfq-email-poller-restartable`. |
| `fix/liveness-quotes-spine-aware` | PR C of liveness silent-failure triage 2026-05-25 | `liveness_checks.py` — new `_quote_ingestion_freshness()` helper reads MAX(created_at) across legacy `quotes` AND Spine `spine_quotes`. Closes the false-alarm class for the Quote ingestion check: per §0 LAW 1 the Spine is canonical, new quotes flow through `spine_quotes`, so reading only `quotes.created_at` triggered an 11d-silent alarm while spine_quotes was current. Collapses to single-table read when legacy `quotes` is DELETED at the end of the Spine migration. 11/11 tests green. Worktree: `rfq-liveness-quotes-spine-aware`. |
| `fix/spine-contract-address-ingest` | MERGED PR #1089 | Mr. Wolf — Duffey R26Q47 PDF rendered `Bill to: CCHCS` + `Ship to: CA STATE PRISON SACRAMENTO` with no street / no PO Box / no email (Mike eyes-on catch 2026-05-26). LAW 6 violation at the ingest seam: `spine_bridge/ingest.py::_build_email_contract` never wrote `bill_to_*` and only echoed raw `ship_to`. Renderer at `quote_pdf.py:579-581` correctly fell back to bare agency+facility. Fix: two resolvers (`_resolve_canonical_bill_to` reads legacy AGENCY_CONFIGS; `_resolve_canonical_ship_to` reads `facility_registry`) wired into `_build_email_contract` so the contract carries every answer at ingest. Raw-dict values still win when present (operator overrides never stomped). 6 existing contracts on prod backfilled via `/contract-override`: Duffey + a2f2643d + Argarin x2 got full bill+ship; Coleman + Ragadio NON-IT got bill-only because `facility=UNKNOWN`. 20/20 ingest tests pass incl. 5 new ones (canonical-resolution, raw-wins, unknown-agency graceful). Worktree: `rfq-spine-save-button-fire`. |
| `fix/rfq-ingest-no-pc-spawn` | MERGED PR #1092 | Mr. Wolf — RFQ-class emails spawned ghost `auto_rfq_*` PCs (5 visible on prod: auto_rfq_d877/f11c/cc83/0124/89bb) because `_trigger_auto_price` was called from BOTH the V2 ingest branch (`dashboard.py:~2271`) AND the legacy `process_rfq_email` tail (`dashboard.py:~3595`) — both produce RFQ records, so the trigger doubled every inbound on /home. 4th-in-5-days `[[feedback-kpi-substrate-singleness]]` instance. Fix: removed both callsites. 5 ghost PCs DELETED from prod data post-deploy. 4 PR-AV9 tests rewritten to pin the NEW contract. Worktree: `rfq-rfq-ingest-no-pc-spawn`. |
| `fix/spine-sticky-price-regression-pin` | Active | Mr. Wolf PR #5 — adds 3 regression tests in `tests/spine/test_state_endpoint_preserves_typed_prices.py` pinning the contract that the `/state` endpoint persists operator-typed `unit_price_cents` verbatim. No code change — the Spine substrate ALREADY behaves correctly per Window 2's empirical Duffey test + client-side `spine_pc_detail.html:543-575` JS that recomputes markup-from-price (operator's typed price preserved). The tests pin server-side stickiness using payloads with explicitly-NOT-25% markups (100% + 0% override) so a future regression to `price = cost × (1 + default_markup)` would flip a 2000-cent typed price to 1250 and fail loudly. Closes Handoff A PR-2 substrate concern: the bug Mike named lives in LEGACY `rfq_detail.html` (which is on §0 LAW 2 deletion schedule); the Spine path he operates is provably clean. 3/3 tests green. Worktree: `rfq-spine-sticky-price-regression-pin`. |
| `chore/claude-md-law-7-governance` | MERGED PR #1090 | Mr. Wolf — adds §0 LAW 7: "§0 changes only by PR; verbal directives MOTIVATE a PR, they don't BECOME one." Closes the failure mode that produced two conflicting handoffs on 2026-05-25. Docs-only; updates section header "six laws" → "seven laws". Worktree: `rfq-claude-md-law-7-governance`. |
| `fix/legacy-tax-render-canonical` | Active | Mr. Wolf — legacy RFQ detail tax label rendered `Tax (0.077%)` instead of `Tax (7.75%)` when `tax_rate` was stored as fraction (0.0775) instead of legacy-canonical percent (7.75). Substrate-singleness contamination — different write paths used different conventions for the same field. Fix: `rfq_detail.html:768` template — defensive normalize matching `pricing_alignment.py:144`'s `>1` rule. <=1 → multiply by 100 (treat as fraction); >1 → use as-is (treat as percent). Both conventions now render correctly. Editor input at L350 left untouched (round-trip with recalc()+autosave is separate scope; this PR is read-only display fix). 4 new tests pin both storage conventions + zero case + district-add-on case. Worktree: `rfq-legacy-tax-render-canonical`. |
| `fix/oracle-substrate-telegram` | Active | Mr. Wolf pass on the 2026-05-25 empty `[ORACLE] Weekly Intelligence` screenshot (0/0/0 KPIs above a 100+-sample calibration table = two substrate tables on one card). Three changes in `src/agents/`: (1) `notify_agent.py` — new `_send_telegram` + MarkdownV2 escaper + `TELEGRAM_*` env vars + `CHANNEL_MAP` reorganized into three tiers (ACTIONABLE sms/email/bell, REPORTS telegram/bell, INTELLIGENCE telegram+email/bell); (2) `oracle_weekly_report.py` — KPI #3 re-sourced from `winning_prices.COUNT(*)` (win-only path, empty in prod) → `SUM(oracle_calibration.sample_size)` and relabeled "Calibration Samples"; (3) `award_tracker.py` — fires `award_tracker_idle` (daily-bucketed) when run_award_check sees 0 eligible but >0 recent quotes (exposes silent Mark-Sent break). Removed explicit `channels=["email"]` from oracle + cross_sell so routing follows the new map. **Patch 4 (Mark-Sent invariant) deferred** — write seams are scattered across dashboard/routes_pc/routes_rfq + Spine `/send-prep`, not in `set_quote_status_atomic`; Architect-level call on where invariant belongs. Idle alarm covers same break. 24 new tests + 286/286 critical green. **Operator todo before merge:** BotFather → `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` + `TELEGRAM_ENABLED=1` on Railway. Channel silently degrades if missing — bell still fires + the three INTELLIGENCE-tier events keep email as backup. Worktree: `Reytech-RFQ` (main checkout). |
| `fix/currency-input-trailing-zeros` | Active | Mr. Wolf — operator screenshot 2026-05-26: RFQ row inputs showed `8.0` / `9.6` / `13.5` instead of `8.00` / `9.60` / `13.50`. Two-seam substrate bug: (1) `rfq_detail.html` cost+price inputs were `type="number"` which strips trailing zeros at the browser; (2) `shared_item_utils.js` helpers `sanitizePrice(this)` / `fmtCurrency(this)` were called as element-mutators at every PC + route call site but the signatures were value-in/value-out, so every onblur "format" was a silent no-op. Fix at three seams: shared helpers overloaded element-aware (mutate `el.value` when called with an HTMLInputElement); `rfq_detail.html:684,720` swap `type="number"` → `text inputmode="decimal"`, server-format value as `%.2f`, replace `width:110px` with `min-width:110px;field-sizing:content` for auto-grow on long prices, wire `sanitizePrice`+`fmtCurrency`; `routes_pricecheck_pricing.py:1307` `ed_price_{i}` same swap. 13/13 new tests pin behavior. Chrome-walked at 1440×900: rendered `8.00`/`9.60`/`13.50`/`12345.67`/`98765.43`; onblur formatter pads `7.5` → `7.50`; sanitizer strips `$1,234.5abc` → `1234.5` → `1234.50`; field-sizing grows 110→123→170px as content lengthens. Worktree: `rfq-currency-input-trailing-zeros`. |
| `fix/unify-scprs-get-conn` | Drafted, awaiting PR #1138 | Mr. Wolf — downstream half of the 2026-05-27 boot-rebuild lock cascade (PR #1138 = upstream freshness gate, this PR = downstream pragma policy). `scripts/run_scprs_harvest.py:get_conn()` opened raw `sqlite3.connect(... timeout=30); PRAGMA busy_timeout=5000` — 6× shorter lock-wait than `src.core.db._make_connection`'s 30000ms, and missing the synchronous=NORMAL + foreign_keys=ON pragmas. Intel rebuild = longest write txn in system; ran with shortest lock-wait. Fix: delegate to `_make_connection` so the rebuild's pragma policy matches every other writer. Same `get_conn` name + signature = zero-churn for callers (CLI + `scprs_intelligence_engine.py:1109` import unchanged). Smoke-tested: busy_timeout 30000, journal_mode wal, synchronous=NORMAL, foreign_keys=ON. **Held local until PR #1138 lands** (no file collision, but sequencing one substrate change at a time per Three Strikes discipline — fire after the freshness gate proves lock cascade is gone). Follow-up sprint: ~60 other `sqlite3.connect` call sites in `src/` + `scripts/` still need the same delegation; out of scope for this PR. Worktree: `rfq-unify-scprs-get-conn`. |
| `fix/facility-registry-canonical-name` | Active | Operator flagged generated quote ship-to displaying "CSP-Sacramento - New Folsom" 2026-05-27 — CSP-SAC and FSP are different prisons and "New Folsom" is a colloquialism that belongs in aliases only. Three edits: (1) `src/core/facility_registry.py:235` canonical_name "CSP Sacramento - New Folsom" → "California State Prison, Sacramento"; (2) same file CIW (L189) + CHCF (L175) parent_agency "CCHCS" → "CDCR" with parent_agency_full updated to match (CCHCS provides healthcare WITHIN CDCR prisons; parent dept per CDCR org chart is CDCR); (3) `src/forms/quote_pdf_edits.py:21-22` docstring example string updated to new canonical. Aliases tuple ("new folsom"/"new folsom prison"/"new folsom state prison" + 6 others) preserved verbatim — INBOUND matching unchanged, only OUTBOUND canonical changes. Bug-pinning test in `tests/test_quote_generator_facility_resolve.py:44` updated. New `TestCanonicalNameConflation` + `TestCdcrParentAgency` classes in `tests/test_facility_registry.py` (canonical no longer contains "New Folsom"; "new folsom" alias still resolves to CSP-SAC; CIW + CHCF parent_agency == "CDCR"). 25/25 facility + 9/9 quote-gen-resolve + 59/59 PDF roundtrip tests green. CDCR online-validated (cdcr.ca.gov/facility-locator/sac/). Worktree: `rfq-facility-registry-canonical-name`. |
| `fix/spine-arch-test-adapter-whitelist` | Active | Implementer PR-C 2026-05-27 — `tests/spine/test_spine_architecture.py::test_no_legacy_imports` was RED after Window 1's Pillar-4 sprint added 8 adapter renderers in `src/spine/agency_forms/` (calrecycle_74 / cchcs_703c / cchcs_704c / cuf / darfur / dvbe_843 / std_1000 / std_204), each delegating to the verified legacy fillers `reytech_filler_v4` or `cchcs_attachment_fillers`. Same documented adapter pattern as `forms_render.py` / `packet_render.py`. Two edits: (1) per-file entries added to `_FILE_SCOPED_LEGACY_IMPORTS` whitelisting each file individually — no blanket `agency_forms/*` rule, so a new renderer added without explicit approval still fails the architecture test; (2) extend `SPINE_CHARTER.md` "Sanctioned Boundary" with a new section "Adapter renderers in `agency_forms/`" reconciling with the 2026-05-20 "retired" wording (retired files were from-scratch fillers; current files are adapters). Architect-approved per CLAUDE.md §0 LAW 4 ("go" 2026-05-27). 8/8 architecture tests green. No source files under `src/spine/agency_forms/` touched. Worktree: `rfq-spine-arch-test-adapter-whitelist`. |
| `fix/job1-test-cchcs-routes-no-legacy` | Active | Implementer PR-Job1-E 2026-05-27 — extends `tests/spine/test_spine_architecture.py` with `test_cchcs_routes_no_legacy_imports`, the arch forcing function for §0 Job #1 acceptance "0 imports from src/core/ in the CCHCS quote path." Covers the Spine HTTP entry point (`src/api/modules/routes_spine.py`). Forbidden seams = `src.core.*` + `src.forms.*` (stricter than `_is_legacy_import` because route files legitimately import `src.api.shared` + `src.spine_bridge`). Existing function-scoped `src.core.paths` import at line 1850 (DATA_DIR for `SPINE_DB_PATH` fallback) whitelisted in `_ROUTE_FILE_SCOPED_LEGACY_IMPORTS` with per-file + per-module precision — same shape as Spine's `_FILE_SCOPED_LEGACY_IMPORTS`. AST import-walk refactored into a shared `_collect_legacy_import_offenders` helper that both tests call with different `is_legacy` predicates; existing test behavior preserved exactly. 9/9 architecture tests green (8 existing + 1 new). Negative-test verified: `from src.core.tax_resolver import resolve_tax` in `routes_spine.py` would be caught. SPINE_CHARTER.md updated with a "CCHCS HTTP entry point" subsection under "Sanctioned Boundary" — Architect-approved per §0 LAW 4 (the ticket itself). Worktree: `rfq-job1-test-cchcs-routes-no-legacy`. |
| `fix/job1-promote-cchcs-billto-to-spine` | Active | Implementer PR-Job1-A0 2026-05-27 — Job #1 prerequisite. Architect-approved per §0 LAW 4. Promotes CCHCS canonical bill-to (CDCR Accounts Payable: name, email, 3-line PO Box address) from the legacy `src/forms/quote_generator.AGENCY_CONFIGS["CCHCS"]` into a new Spine-native module `src/spine/agency_constants.py` (frozen `CchcsCanonicalBillTo` dataclass + `cchcs_bill_to_tuple()` helper). `_resolve_canonical_bill_to` in `src/spine_bridge/ingest.py` now reads CCHCS from the Spine source of truth; other agencies (CDCR/CalVet/DSH/DGS) still read from legacy AGENCY_CONFIGS unchanged (out of Job #1 scope). Unblocks PR-Job1-A (delete `AGENCY_CONFIGS["CCHCS"]`) which would otherwise silently strip bill_to_* from every new CCHCS EmailContract (LAW 6 violation). SPINE_CHARTER.md extended with "Spine-native agency constants — `agency_constants.py`" section. Output verified byte-for-byte identical to legacy. New test file `tests/spine/test_agency_constants.py` pins values + frozen shape; `tests/spine/test_ingest_email_contract.py` extended to assert Spine-native source. Worktree: `rfq-job1-promote-cchcs-billto-to-spine`. |
| `fix/job1-delete-agency-configs-cchcs` | Active | Implementer PR-Job1-A 2026-05-27 — §0 Job #1 LAW 2 deletion gate. Deletes the `AGENCY_CONFIGS["CCHCS"]` entry (~10 LOC) at `src/forms/quote_generator.py:84-92`. Unblocked by PR-Job1-A0 (merged commit `1ccf9c22`) which promoted CCHCS canonical bill-to (name, email, 3-line PO Box address) into Spine-native `src/spine/agency_constants.py` + rewired `src/spine_bridge/ingest.py::_resolve_canonical_bill_to` to read from the Spine source. Runtime parity verified pre- AND post-delete: `_resolve_canonical_bill_to('CCHCS')` returns the SAME canonical tuple (`'Dept. of Corrections and Rehabilitation'`, `'APA.Invoices@cdcr.ca.gov'`, `'Attn: Accounts Payable\nP.O. BOX 187021\nSacramento, CA 95818-7021'`) before and after the delete — LAW 6 preserved across the deletion. Sibling keys (`CDCR`/`CalVet`/`DGS`/`DEFAULT`) survive — multi-agency entries deferred to per-agency migrations. `tests/test_quote_generator.py:89-92` rewritten from `test_cchcs_no_bill_to_no_permit` (read-the-entry) to `test_cchcs_not_in_agency_configs` (defense-in-depth pinning the deletion — any future re-add must explicitly delete this test, surfacing the LAW 2 regression at PR review). 82/82 tests green: `tests/test_quote_generator.py` (48 in file) + `tests/spine/test_ingest_email_contract.py` (20) + `tests/spine/test_agency_constants.py` (8). `git grep AGENCY_CONFIGS["CCHCS"]` returns 0 src/ code hits (only docstring/comment references in agency_constants.py + ingest.py + SPINE_CHARTER.md remain). Worktree: `rfq-job1-delete-agency-configs-cchcs`. |
| `fix/ingest-cross-type-thread-dedup` | Active | Mr. Wolf — Chechi NON-IT orphan `rfq_cd41cfee` landed in queue tonight despite operator-dismissed `rfq_011bf33b` earlier. Triangulated: NOT the same row resurfacing — TWO different Gmail threads (PREQ 10846357 + 10847262), both buyer-reply emails mis-classified as fresh RFQ orphans. TWO substrate bugs at `src/core/ingest_pipeline.py`: (1) `_find_active_record_by_thread("rfq", ...)` queried `WHERE created_at >= ?` against `rfqs` — but `rfqs` has NO `created_at` column (uses `received_at`/`updated_at`). SQLite raised OperationalError on every RFQ-side dedup; helper's `except Exception` swallowed silently → returned None → RFQ thread-dedup was **dead code** since the helper landed. (2) Even when same-type works, parents on the OTHER table (Chechi 10846357 thread has 3 PCs + 1 sent RFQ + 1 dismissed orphan in `rfqs`) were invisible to per-type lookup. Fix: rewrite per-type helper to use per-table sort_ts (`COALESCE(received_at, updated_at, '')` for rfqs, `COALESCE(created_at, received_at, '')` for pcs), drop the silently-failing cutoff. Add new `_find_active_record_on_thread_any_type` that UNIONs both tables — called from BOTH ingest branches as safety net after same-type. Sets `status='duplicate'` + `dedup_of` + `gmail_thread_duplicate_of` so Python `is_active_queue` AND SQL view `v_active_queue_rfqs` both filter the orphan. 12/12 new tests + 57/67 RFQ+PC+golden regression green. Backfill: operator dismisses `rfq_cd41cfee` via `×` button (one click); fix deploy prevents next orphan. **6th substrate-singleness instance this week** — class lesson: silent exception handlers around SQL queries hide schema-column drift indefinitely; helpers must use schema-correct columns OR error loudly. Worktree: `rfq-ingest-cross-type-thread-dedup`. |

## Preserved branches — await Closer triage

Job #0 kept 28 branches that have unmerged commits and no merged PR, plus the
6 explicitly preserved in Step 1. They are NOT abandoned and NOT confirmed
live — the Closer triages each (live vs dead) before Job #1. Full list:
`_diag/job0_branches_kept.txt`. Known callouts: `feat/spine-shadow-ingest-and-bidpkg`
— Architect flagged as possibly superseded by PR #1033; `feat/spine-signature-overlay`
— memory says do NOT ship.

## Coordination rules

1. Read this file before creating a branch.
2. One window = one branch = one PR. Never push directly to `main`.
3. Update the table on branch create / PR / merge / abandon.
4. `make ship` is the only way to push.

## Conflict zones

Frequently-edited files — if two windows need the same one, sequence the work:
- `src/api/dashboard.py`, `src/api/data_layer.py`
- `src/api/modules/routes_rfq.py`, `src/api/modules/routes_pricecheck.py`
- `src/spine/model.py` (Architect-only), `src/spine/SPINE_CHARTER.md`
- `CLAUDE.md` (coordinate edits)
