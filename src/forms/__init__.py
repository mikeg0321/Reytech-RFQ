"""PDF form parsing, filling, and quote generation.

Key exports:
    parse_ams704()          — Parse AMS 704 price check forms
    fill_ams704()           — Fill 704 with pricing and supplier info
    generate_quote()        — Generate Reytech branded quote PDF
    generate_quote_from_pc() — Generate quote from a price check
    parse_rfq_attachments() — Parse 703B/704B RFQ attachment pairs
"""
