__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.env
.venv/
env/
venv/
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
uploads/
output/
artifacts/
tmp/

# ── Local E2E verification artifacts ─────────────────────────────────────────
docs/screenshots/
data/shadow_diffs.jsonl
data/shadow_outputs/
data/tmp*.tmp

# ── Sensitive / auth tokens (never commit) ───────────────────────────────────
data/qb_tokens.json
data/qb_vendors.json
data/.write_test

# ── Email corpus (PII, buyer names, prices — never commit raw data) ──────────
data/email_corpus/
data/buyer_corpus/

# ── High-volume transient logs (too large / rebuilt at runtime) ───────────────
data/processed_emails.json
data/email_outbox.json
data/email_sent_log.json
data/scprs_scan_log.json
data/scprs_seen_pos.json
data/workflow_runs.json
data/audit_log.json
data/item_id_cache.json
data/leads.json
data/lead_history.json
data/gmail_health.json

# ── SQLite DBs live on Railway volume, not in git ────────────────────────────
data/reytech.db
data/reytech.db-wal
data/reytech.db-shm
data/qa_intelligence.db
data/rfq.db
data/spine.db
data/spine.db-wal
data/spine.db-shm
data/scprs_harvest.log
data/reminded_ids.json

# ── Generated PDFs / reports (rebuilt at runtime) ────────────────────────────
data/backups/
data/Quote_*.pdf
data/PC_*.pdf
data/tax_rate_cache.json
data/growth_report.pdf
data/growth_report.xlsx

# ── Migration artifacts ──────────────────────────────────────────────────────
data/*.migrated

# ── Walkthrough / theme screenshots (local verification artifacts) ──────────
.inspector_shots/
.theme_shots/

# ── Claude Code (ignore settings, keep shared coordination files) ───────────
.claude/*
!.claude/WORKSTREAMS.md
!.claude/agents/
!.claude/agents/**
!.claude/settings.json
!.claude/hooks/
!.claude/hooks/**

# ── GitHub (ignore IDE config, keep workflows) ─────────────────────────────
.github/*
!.github/workflows
_diag/
_overnight_review/
deadline_snooze_screenshot.png
tmp_audit/
baseline_pc_detail_pre_pr3a.png
baseline_*.png
.chrome_verify/
.chrome-verify/

# ── Per-session working-tree noise (audit drafts, verify screenshots, etc.) ──
# Keeps Mike's local audit notes out of git unless explicitly committed.
.audit_tmp/
chrome-verify/
data/verify/
data/verify_*.png
data/tax_rates_cache.json
scripts/_prod_orphan_triage.py
scripts/schedule/
# Per-bid one-off renderer escape-hatch scripts (operator-local; never
# committed — they bypass the spine ingest path by design).
scripts/gen_chcf_*.py

# ── Old audit doc variants (superseded by docs/AUDIT_DEEP_E2E_2026_05_07_v2.md) ──
# Future audit work should overwrite the canonical v2 doc; these stale
# variants stay local-only. Commit a new canonical doc deliberately if needed.
docs/AUDIT_DEEP_E2E_2026_05_07.md
docs/AUDIT_FULL_E2E_2026_05_07.md
docs/AUDIT_HANDOFF_2026_05_07.md
docs/AUDIT_HOME_QUOTING_GROWTH_2026_05_06.md

# Ephemeral test-fire scripts (run against prod via `cat | railway ssh`).
.tf_*.py

# Chrome-MCP verification screenshots saved during audit ships.
rfq_*_verify*.png
pc_*_verify*.png
*_verify_top.png
captures/
