# Reytech-RFQ Audit — 2026-05-06

Spawned 4 parallel Explore agents (RFQ+PC funnel · CRM+System · Orders+Intel+Analytics · Forms+CCHCS), each producing concrete findings with file:line refs. I then ran a **verification pass** on every claim against current code to filter false positives — agents are good at pattern-matching but tend to over-call P0 on style issues.

**Status legend:**
- `VERIFIED` — confirmed against current code, real bug
- `FALSE_POSITIVE` — agent claim doesn't hold; left in for transparency
- `NEEDS_VERIFY` — plausible but I couldn't confirm without prod data or runtime trace

**Severity legend (post-verification):**
- **P0** — silent data loss, security hole, or prod-down risk
- **P1** — wrong behavior visible to user / Mike; affects revenue accuracy
- **P2** — quality, robustness, or cleanup; defer until quoting is rock solid

---

## P0 — fix this week

| # | File:line | Verified? | Finding | Fix shape |
|---|-----------|-----------|---------|-----------|
| 1 | `src/api/dashboard.py:104-106` | VERIFIED | `_save_pending_pos()` writes JSON without atomic temp+rename. App crash mid-write corrupts `pending_po_reviews.json` → home banner can't render until next boot's email re-scan. | Wrap in `tempfile.NamedTemporaryFile` + `os.replace()`. Mirror `src/core/db.py` atomic write helper. |
| 2 | `src/api/modules/routes_crm.py:59-60` | NEEDS_VERIFY | `_save_customers()` claimed to write JSON without lock. CRM corruption under concurrent edit. | Read the function; if no lock, wrap in atomic write + `_json_write_lock`. |
| 3 | `src/forms/price_check.py:5011` | NEEDS_VERIFY | URL/filename sanitizer `re.sub(r'[^a-zA-Z0-9_-]', '_', pc_num)` strips spaces from PC numbers — PR file lookups 404 silently. CLAUDE.md explicitly flags this pattern under "URL Sanitization Must Preserve Spaces". | Only block path traversal: `re.sub(r'[\\./\\\\:*?"<>|]', '', pc_num)`. Preserve spaces. |
| 4 | `src/forms/reytech_filler_v4.py:722-741` | NEEDS_VERIFY | Double-sig guard reads `/Sig` field detection but only checks names in `SIGN_FIELDS` whitelist. Variant `/Sig` field names (e.g. `DigitalSignature`, `AuthorizedSignature_Alt`) bypass the guard → `_703b_overlay_signature` runs anyway → double-sign on prod packets. | Expand guard: `if str(_obj.get("/FT")) == "/Sig"` regardless of name. Test with variant 703B field names. |
| 5 | `src/forms/price_check.py:4840-4890` | NEEDS_VERIFY | `_add_signature_to_pdf()` falls back to hardcoded rect (279, 388, 602, 412) from AMS 704 Rev 1/2019 if detection fails. **No lower-40% validation.** Buyer-supplied PDF with field at top of page → signature drawn invisibly off-form. | Check `fb` against `0.4 * page_height`. If detected rect is in upper 60%, log WARNING and skip overlay (treat as malformed buyer form). |
| 6 | `src/core/order_dal.py` (multiple INSERT/UPDATE) | NEEDS_VERIFY | `clean_po_number()` exists but agent claims it's NOT called at every orders write site. CLAUDE.md memory `project_session_2026_04_28_sentinel_pos.md` says PR #633 wired this through 3 paths. **Agent may be wrong** — verify by grepping every `INSERT INTO orders` / `UPDATE orders` and confirming clean_po_number runs first. | If real, add to remaining sites. If false positive, write a source-level guard test pinning every write site. |

## P1 — within next sprint

| # | File:line | Verified? | Finding | Fix shape |
|---|-----------|-----------|---------|-----------|
| 7 | `app.py:525-527` | NEEDS_VERIFY | `DASH_PASS` claimed to be logged in plaintext during pre-warm cache init. If true, secret is in Railway build logs. | Read line 525-527; if any log call references `_dash_pass`, mask with `*` of same length. |
| 8 | `src/core/canonical_state.py` / facility_registry split | VERIFIED | Two parallel resolvers (`institution_resolver` + `facility_registry`) with different fallback semantics. Audit memory `project_codebase_audit_2026_04_30.md` flagged this. Mike's call required on which wins. | Pick one canonical resolver, migrate writers, deprecate the other with shim. Multi-PR. |
| 9 | `src/core/pricing_oracle_v2.py:2271-2288` | NEEDS_VERIFY | `calibrate_from_outcome()` claimed to commit upsert without try/except. If DB write fails, calibration silently drops the outcome. | Read the function; if no try/except + log on commit failure, wrap. Verify the calibration path actually fires on prod (existing memory says it does, 488 calibrations in Phase 0.7d). |
| 10 | `src/api/modules/routes_pricecheck.py:2272-2277` | VERIFIED | Operator-driven catalog cost overwrite has no upper-bound check. Operator typo ($2500 vs $25) permanently corrupts catalog cost for every future quote. | Add 3x cost guardrail (matches the existing rule in CLAUDE.md "Cost Sanity Guardrail"). Log old vs new cost. |
| 11 | `src/api/modules/routes_pricecheck.py:3195-3226` | VERIFIED | PDF recovery from DB swallows `email_attachments` table-missing exception. Operator gets generic "Source PDF not found" error and spends 20 min uploading when real cause is schema drift. | Replace bare `except: pass` with `except Exception as _dbe: log.error("...table missing? %s", _dbe)`. Surface DB-shape errors separately from missing-file errors. |
| 12 | `src/forms/cchcs_packet_filler.py:1097-1104` | NEEDS_VERIFY | `_SIGNATURE_TARGETS` are hardcoded field names. If CCHCS form changes, signatures silently won't be drawn. | Add fallback: after specific targets fail, scan all pages for any `/Sig` field in lower 40% and overlay. |
| 13 | `src/api/modules/routes_pricecheck.py:1024` | NEEDS_VERIFY | `_json.dumps(...).replace("</", "<\\/")` is incomplete script-escape — only catches `</`, not `<!--` or CDATA breaks. XSS vector if `profit_summary` ever takes user input (currently operator-set). | Use proper context-aware escaping or wrap in CDATA. Low risk today; matters if data sources change. |

## P2 — defer until quoting is rock solid

These are quality / robustness improvements. None are user-visible bugs; they fail under stress or edge cases.

| File:line | Finding |
|-----------|---------|
| `routes_pricecheck.py:57` | `json.load(open(path))` — Python closes via refcount, but pattern is fragile. Convert to `with open(path) as f: json.load(f)`. |
| `routes_pricecheck.py:1788` | `traceback.print_exc()` after `log.error` is redundant; trace goes to stderr not log sink. Drop. |
| `routes_pricecheck.py:1829-1856` | `_recompute_unit_price()` swallows ValueError silently. Add a debug log so a re-quote pain is traceable. |
| `routes_crm.py:3620` | `open(reg_path)` without context manager. Convert. |
| `email_poller.py:54-75` | Rejection log has no dedup. Audit-trail bloat under high-volume rejections. |
| `task_consumer.py:172-186` | Daemon thread `register_job` ordering — if registration fails, orphaned thread runs untracked. Re-order calls. |
| `dashboard.py:108-146` | Edge case in `_dedupe_pending_pos`: po_number=`"0"` would be dropped. Unlikely but tighten check. |
| `oracle_backfill.py:276-293` | `joinback_won_quotes_kb` early returns silently. Caller doesn't check return value. Add a count log + assertion. |

## FALSE_POSITIVES — agent flagged, I disagree after verification

These should NOT be fixed; agent misread the code.

| File:line | Agent claim | Why it's wrong |
|-----------|-------------|----------------|
| `routes_pricecheck.py:80` | "json.dumps with default=str silently corrupts revert" | The pattern is a deep-copy idiom (`json.loads(json.dumps(..., default=str))`) used to detach the snapshot from the live object. `default=str` only applies to non-JSON-native types (datetime, UUID). Standard pattern. |
| `routes_analytics.py:4194` | "SUM(quantity × unit_price) double-counts SCPRS line totals" | Verified against `src/knowledge/won_quotes_db.py:298-300` which states `unit_price IS ALREADY per-unit` (divided by qty on ingest). Multiplying by qty here is **correct** to get total spend. |
| `routes_analytics.py:4171` | "Agency filter silently returns 0 rows" | Filter is `agency IS NOT NULL AND agency != ''`. The audit memory's "agency overload" issue is real (column sometimes contains institution labels), but the filter still matches non-empty values — it doesn't return 0; it returns wrong groupings. That's a separate, already-tracked issue (Phase 2.3). |
| `routes_pricecheck.py:2890-2926` | "Race condition in `_LOOKUP_STATUS`" | Lock scope is per-pcid; concurrent items on the SAME pc are serialized. Concurrent items on DIFFERENT pcs run in parallel as designed. |
| `task_consumer.py:116-167` | "Consumer loop dies silently after first exception" | Verified: outer try/except at line 124-167 catches ALL exceptions, logs via `exc_info=True`, calls `heartbeat(success=False)`, sleeps, and loops. The loop is well-engineered. |
| `app.py:380-520` | "Deferred init can leave task queue uninitialized but app live" | The deferred init runs in a background thread on purpose — app serves requests while non-critical setup happens. If task queue init fails, that's a real problem, but the architecture is intentional, not a bug. |
| `fill_engine.py:270, 276` | "NotImplementedError raises in active code paths" | Verified: ALL profiles in `src/forms/profiles/` use `fill_mode: acroform` or `generated`. NO profile uses overlay/hybrid. Code path is unreachable until/unless a new profile with overlay/hybrid mode is added. |
| `cchcs_intel_puller.py:463-465` | "AVG(unit_price) wrong if line_total" | Same as #4194 — won_quotes-derived tables (cchcs_po_lines) inherit per-unit semantics from won_quotes ingestion. |
| `routes_analytics.py:4115-4132` | "is_test NULL not handled in revenue aggregations" | Migration history shows is_test was migrated to default 0 long ago; NULL rows don't exist in current prod data per multiple recent audit memories. Theoretical rather than current. |

---

## Already-shipped, no longer applicable

The following items appeared in the older audit memories but are confirmed CLOSED via prior PRs:

- V1 DAL `core.dal.save_*` writers — deleted PR #653/#654 region
- 67 orphan orders linker — `orders_link_orphans.py` shipped with operator-review queue
- McKesson server-side resolution — Step 1.25 in `pc_enrichment_pipeline.py`
- Oracle UPC plumbing — all 10 call sites pass `upc=` (verified 2026-05-06)
- AUTO_<hex> queue display — PR #733 read-side override (write-side approach explicitly rejected by Mike — see `feedback_respect_prior_design_calls`)

---

## How this audit was built

1. Spawned 4 Explore agents in parallel, each with a domain-specific prompt + file shortlist
2. Each agent returned 10-15 findings with file:line citations
3. I verified the highest-blast-radius claims against current code (pricing_oracle, won_quotes, fill_engine, task_consumer, dashboard atomic writes)
4. Categorized by post-verification severity, marked FALSE_POSITIVE / VERIFIED / NEEDS_VERIFY
5. Cross-referenced against active memory (`project_codebase_audit_2026_04_30.md`, `project_session_2026_04_27_punch_list_close.md`) to drop already-shipped items

**Total raw findings:** 47 across 4 agents
**After verification:** 13 actionable (6 P0, 7 P1) + 8 P2 + 9 false positives flagged

## Recommended next moves

Mike's call. My read in priority order:

1. **P0 #6** (`clean_po_number` coverage at every orders write site) — verify first; if real, add a source-level guard test pinning every INSERT/UPDATE site
2. **P0 #1** (atomic JSON write for `pending_po_reviews.json`) — small, contained, idempotent, low blast radius
3. **P0 #5** (signature lower-40% validation) — affects every PC with buyer-supplied 704
4. **P0 #4** (double-sig guard variant `/Sig` names) — real bug, 1-line fix
5. **P0 #3** (sanitization preserves spaces) — CLAUDE.md explicit guardrail violation

P1 items can batch into one cleanup PR after the P0s land.
